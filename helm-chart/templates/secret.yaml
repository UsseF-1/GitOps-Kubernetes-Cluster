apiVersion: v1
kind: Secret
metadata:
  name: {{ include "voting-app.secretName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "voting-app.labels" . | nindent 4 }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.secret.postgres.password }}
