apiVersion: apps/v1
kind: Deployment
metadata:
  name: result-deploy
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "voting-app.labels" . | nindent 4 }}
    {{- include "voting-app.selectorLabels" (dict "component" "result") | nindent 4 }}
spec:
  replicas: {{ .Values.result.replicaCount }}
  selector:
    matchLabels:
      {{- include "voting-app.selectorLabels" (dict "component" "result") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "voting-app.labels" . | nindent 8 }}
        {{- include "voting-app.selectorLabels" (dict "component" "result") | nindent 8 }}
    spec:
      containers:
        - name: result
          image: {{ include "voting-app.image" (dict "registry" .Values.registry "image" .Values.result.image) }}
          imagePullPolicy: {{ .Values.result.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.result.containerPort }}
              name: result-port
          env:
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "voting-app.configMapName" . }}
                  key: POSTGRES_HOST
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: {{ include "voting-app.configMapName" . }}
                  key: POSTGRES_USER
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: {{ include "voting-app.configMapName" . }}
                  key: POSTGRES_DB
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "voting-app.secretName" . }}
                  key: POSTGRES_PASSWORD
          resources:
            {{- toYaml .Values.result.resources | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.result.readinessProbe | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.result.livenessProbe | nindent 12 }}
